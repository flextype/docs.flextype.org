---
title: Cache
breadcrumbs:
  1:
    title: "Core Concepts"
    link: "[url]/en/core/"
on_this_page:
  1:
    title: "Methods"
    link: "methods"
---

### <a name="methods"></a> Methods

| Method | Description |
|---|---|
| <a href="#getCacheDriver"><code>getCacheDriver()</code></a> | Get Cache Driver |
| <a href="#driver"><code>driver()</code></a> | Returns driver variable |
| <a href="#getKey"><code>getKey()</code></a> | Get cache key. |
| <a href="#fetch"><code>fetch()</code></a> | Fetches an item from the cache. |
| <a href="#contains"><code>contains()</code></a> | Checks if an config item with this key name is in the config. |
| <a href="#delete"><code>delete()</code></a> | Checks if an config item with this key name is in the config. |
| <a href="#clear"><code>clear()</code></a> | Checks if an config item with this key name is in the config. |
| <a href="#clearAll"><code>clearAll()</code></a> | Checks if an config item with this key name is in the config. |
| <a href="#setLifetime"><code>setLifetime()</code></a> | Checks if an config item with this key name is in the config. |
| <a href="#getLifetime"><code>getLifetime()</code></a> | Checks if an config item with this key name is in the config. |
